__pycache__/
*.pyc
.venv/
venv/
.env
# Processed splits are regenerable via `python src/data_pipeline.py` -- no need to commit them
data/processed/*.pkl
# Model files (~4MB total) ARE committed intentionally -- app.py loads them
# directly at runtime and Streamlit Cloud doesn't run train.py on deploy.
.streamlit/secrets.toml
.DS_Store
